package net.digitalingot.feather;

public interface pm {
    gb<pm> eq = yd.ay(pm.class, pmArray -> () -> {
        for (pm pm2 : pmArray) {
            pm2.render();
        }
    });

    void render();
}
