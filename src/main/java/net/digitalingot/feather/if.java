package net.digitalingot.feather;

import org.jetbrains.annotations.NotNull;
import org.jetbrains.annotations.Nullable;

/*
 * Illegal identifiers - recommend switching to table mode
 */
public class if{
@NotNull
private final List<Integer> Bw=new ArrayList<Integer>();
@NotNull
private final Map<UUID, rf> Bx=new HashMap<UUID, rf>();
@NotNull
private final Map<UUID, ay> By=new HashMap<UUID, ay>();
@Nullable
private gk hG;

private if(){
        }

@NotNull
public static if K(){
        if if_=new if();
        gw gw2=zz.nw().pv();
        gw2.ay(ao.REQUEST_P2P_JOIN,new hq.rf(if_));
        gw2.ay(ao.ACCEPT_P2P_JOIN,new hq.ay(if_));
        return if_;
        }

@NotNull
public List<Integer> L(){
        return this.Bw;
        }

@NotNull
public Map<UUID, rf> M(){
        return this.Bx;
        }

@NotNull
public Map<UUID, ay> N(){
        return this.By;
        }

@Nullable
public gk if(){
        return this.hG;
        }

public void ay(@NotNull gk gk2){
        this.hG=gk2;
        }
        }
