package net.digitalingot.feather;

public interface hl {
    gb<hl> bo = yd.ay(hl.class, hlArray -> () -> {
        for (hl hl2 : hlArray) {
            hl2.onReload();
        }
    });

    void onReload();
}
